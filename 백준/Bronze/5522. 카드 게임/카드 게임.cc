#include<iostream>
#include<vector>
#include<set>
#include<stack>
#include<deque>
#include<sstream>
using namespace std;

int main() {
		int n = 0;
	for (int i = 0; i < 5; i++)
	{
		int input;
		cin >> input;
		n += input;
	}
	cout << n;
}